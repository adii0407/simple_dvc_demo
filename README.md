create env
 
 ```bash
 conda create -n wineq python=3.9
```
 activate env
 ```bash

conda activate wineq
```
created a req file
```bash
pip install -r requirements.txt

git init


dvc init

dvc add data_given/winequality.csv

git add .

git commit -m 'first commit'

git add . && git commit -m "update Readme.md"

git remote add origin https://github.com/adii0407/Simple_dvc_Demo.git

git branch -M main

git push origin main    